# قواعد وإرشادات تطوير منظومة مكتب صحة سفلاق (Saflaq Health Office ERP)

## القاعدة الأساسية الإلزامية — حماية البيانات الحالية والحركات والأرصدة (Production Data Protection)

> [!CRITICAL]
> **قاعدة عدم التدمير (NON-DESTRUCTIVE / PRODUCTION MANDATE)**
> هذا البرنامج مستخدم فعلياً ويحتوي على بيانات حقيقية، دفاتر مسلسلة، حركات وأرصدة تاريخية معتمدة لمكتب صحة سفلاق.
> تعتبر قاعدة البيانات الحالية **بيانات إنتاج Production Data** قطعية.
> **DO NOT MODIFY EXISTING PRODUCTION DATA.**
> **DO NOT DELETE EXISTING TRANSACTIONS.**
> **DO NOT CHANGE EXISTING BALANCES.**
> **DO NOT RECALCULATE HISTORICAL BALANCES AUTOMATICALLY.**
> **DO NOT DROP OR TRUNCATE EXISTING TABLES/COLLECTIONS.**
> **DO NOT REPLACE THE EXISTING DATABASE.**
> **PRESERVE ALL EXISTING DATA AND TRANSACTIONS.**

---

### 1. ممنوع تغيير أو تصفير البيانات الحالية
عند إضافة أي وظيفة أو تعديل أي شاشة أو تطوير أي جزء من البرنامج:
* لا تقم بحذف أي سجل موجود نهائياً.
* لا تقم بتعديل أي حركة تاريخية موجودة.
* لا تقم بإعادة إنشاء قاعدة البيانات من الصفر.
* لا تقم بعمل Reset أو Seed للبيانات الموجودة إذا كانت تحتوي على حركات.
* لا تقم بمسح الجداول أو استبدالها.
* لا تقم بإعادة حساب الأرصدة التاريخية بطريقة قد تغير النتائج الحالية المعتمدة.
* لا تغير أرقام الحركات أو المعاملات أو أرقام الاستمارات المسلسلة.
* لا تغير تواريخ أو قيم الحركات القديمة.
* لا تغير الرصيد الافتتاحي أو الأرصدة الحالية تلقائياً.
* لا تغير أسماء أو معرفات السجلات بطريقة تؤثر على العلاقات بينها.
* لا تغير أي بيانات إلا إذا طلب المستخدم ذلك صراحةً وبشكل واضح لا لبس فيه.

---

### 2. التطوير التراكمي المتوافق رجعياً (Additive & Backward Compatible)
أي تعديل جديد يجب أن يكون متوافقاً بالكامل مع البيانات الموجودة حالياً:
* إذا احتاج التعديل إلى أعمدة أو جداول أو خصائص جديدة: **أضفها فقط (Additive)**.
* لا تحذف الحقول أو الأعمدة القديمة.
* لا تغير معنى الأعمدة الموجودة.
* لا تغير أسماء الحقول الحالية إلا بعد التأكد التام من جميع الأماكن التي تستخدمها.
* اجعل الحقول والخصائص الجديدة اختيارية (Optional with fallback defaults).
* يجب أن يستمر البرنامج في قراءة وعرض البيانات القديمة بدون أدنى خطأ.

---

### 3. حماية الأرصدة المعتمدة
الأرصدة الحالية تعتبر نتيجة معتمدة ومطابقة لدفاتر الجرد والمخزن:
* لا تعد بناء الرصيد من الصفر تلقائياً.
* لا تغير الرصيد الحالي بسبب تحديث كود البرنامج.
* لا تغير طريقة حساب الرصيد القديم إلا إذا طلب المستخدم ذلك صراحة.
* إذا كانت هناك حاجة لطريقة حساب جديدة، أنشئ منطقاً جديداً منفصلاً ولا تعدل البيانات التاريخية.
* يجب التحقق من ثبات الأرصدة قبل وبعد أي تعديل لضمان عدم حدوث أي تغيير غير مقصود.

---

### 4. مناعة الحركات التاريخية (Immutable Transactions)
الحركة التاريخية (صرف، توريد، استمارة، محضر جرد) غير قابلة للتغيير:
* لا يجوز للكود الجديد حذف حركة أو تعديل قيمتها أو تغيير تاريخها أو تغيير نوعها أو رقمها أو طرفها أو تأثيرها المخزني.
* إذا احتاج النظام إلى تصحيح حركة، يتم استخدام **حركة تصحيحية جديدة / Adjustment Transaction** مستقلة تسجل التاريخ والمستخدم والسبب بدلاً من تعديل الحركة الأصلية.

---

### 5. سلامة الـ Migrations والتحولات الهيكلية
إذا كان التعديل يحتاج إلى ترحيل أو توسيع في بنية البيانات:
1. فحص البنية الحالية أولاً وقراءة كافة السجلات الموجودة.
2. تنفيذ Migration تراكمي غير هدمي (Non-Destructive Migration).
3. أخذ لقطة احتياطية تلقائية (Backup Snapshot) قبل أي عملية تحويل.
4. عدم استخدام DROP أو TRUNCATE أو مسح جماعي.
5. لا تنفذ Migration هدمياً على بيانات الإنتاج دون تأكيد وموافقة صريحة.

---

### 6. النسخ الاحتياطي الدائم وتعدد الطبقات
* توفير نسخ احتياطي تلقائي مشفر في LocalStorage و IndexedDB وسحابياً عند الاتصال.
* إمكانية تصدير واسترجاع ملفات الـ JSON في أي لحظة.
* عدم الكتابة فوق النسخ الاحتياطية القديمة تلقائياً، والاحتفاظ بلقطات زمنية موثقة بالتاريخ والوقت.

---

### 7. منع التعديلات الصامتة (No Silent Modifications)
ممنوع إطلاقاً إجراء أي تعديل على البيانات المخزنة ضمن:
* تشغيل البرنامج أو فتح الصفحة (Mounting).
* تسجيل الدخول أو التبديل بين التبويبات.
* تحديث الصفحة (Refresh) أو تحديث كود التطبيق.
* إضافة واجهات أو ميزات جديدة.
أي تغيير في البيانات يجب أن يكون حصراً نتيجة إجراء صريح ومقصود من المستخدم عبر واجهة التطبيق.

---

### 8. المعاملات الذرية والتعامل مع الأخطاء (Atomic Operations)
عند تسجيل حركة جديدة:
* إما يتم حفظ العملية كاملة بنجاح (تسجيل الحركة + استقطاع الرصيد + توثيق المسلسل)، أو إلغاؤها بالكامل بدون تجزئة.
* يمنع منعاً باتاً خصم رصيد دون تسجيل الحركة، أو العكس.

---

### 9. سجل العمليات والتدقيق (Audit Trail)
كل عملية جديدة توثق تلقائياً بالبيانات التالية:
* كاتب الصحة / المستخدم المسؤول.
* التاريخ والوقت بالثانية.
* نوع الحركة (صرف، إضافة، تسوية، إلغاء، تصحيح).
* المعرف والسجل المتأثر.
* الملاحظات والسبب إن وجد.

---

### 10. بروتوكول تنفيذ أي طلب مستقبلي
قبل كتابة أي سطر كود، يتم التحقق من:
1. ما الملفات والواجهات التي ستتأثر؟
2. هل التعديل يؤثر على الأرصدة أو الحركات التاريخية؟ (الجواب يجب أن يكون: لا).
3. هل التعديل Additive تماماً؟
4. اختيار **أقل تعديل ممكن Minimal Change** لتحقيق المطلوب بأعلى دقة وكفاءة.
